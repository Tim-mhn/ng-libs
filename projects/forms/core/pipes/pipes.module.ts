import { NgModule } from '@angular/core';
import { ValidationErrorToMessagePipe } from './validation-error-to-message.pipe';

@NgModule({
  declarations: [ValidationErrorToMessagePipe],
  imports: [],
  exports: [ValidationErrorToMessagePipe],
})
export class TimFormsPipesModule {}
