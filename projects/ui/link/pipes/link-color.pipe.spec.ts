import { LinkColorPipe } from './link-color.pipe';

describe('LinkColorPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkColorPipe();
    expect(pipe).toBeTruthy();
  });
});
