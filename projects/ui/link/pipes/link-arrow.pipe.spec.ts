import { LinkArrowPipe } from './link-arrow.pipe';

describe('LinkArrowPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkArrowPipe();
    expect(pipe).toBeTruthy();
  });
});
